# Source bundle

The OBJ and MTL files here are the generator's output as delivered to Rusty
(`Trailblazer_Animal_Trailer_Asset`, 2026-09), committed unchanged so the pipeline in
`build.py` is reproducible from the repository alone. The bundle has no texture; the
pipeline makes one from the MTL colours. Original work, all rights ours.
